[package]
name = "vanityback"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.7" # Or the latest version
tokio = { version = "1", features = ["full"] }

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
